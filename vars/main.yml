---
# vars file for git

git_config_scope: file
git_user_home_path_default: "{{ git_user_home_path_by_os[ansible_system] }}"
git_user_home_path_by_os:
  Darwin: "/Users/{{ git_user_id }}"
  Linux: "/home/{{ git_user_id }}"
  Windows: "c:/Users/{{ git_user_id }}"

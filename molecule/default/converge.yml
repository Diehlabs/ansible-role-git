---
- name: Converge
  hosts: all
  tasks:
    - name: Add the user needed to test role with defaults
      ansible.builtin.user:
        name: chris

    - name: "Include git"
      include_role:
        name: "git"
